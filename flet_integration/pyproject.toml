[project]
name = "reclaimmychats-flet-integration"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["flet==1.0.0", "reclaimmychats>=3.2"]

[tool.flet.dev_packages]
reclaimmychats = ".."

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
